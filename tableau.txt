.table-layout {
    display: flex;
    align-items: flex-start;
    justify-content: center;
    gap: 30px;
    margin-top: 30px;
}

.table-container img {
    max-width: 800px;
    height: auto;
}

/* Panneau infos */
#info-panel {
    width: 300px;
    min-height: 300px;
    background: #f4f4f4;
    border-radius: 12px;
    padding: 15px;
    box-shadow: 0 0 10px rgba(0,0,0,0.1);
    display: none; /* il apparaît quand on clique */
}
